diff --git a/apps/client/src/features/editor/components/database-table/database-table-view.module.css b/apps/client/src/features/editor/components/database-table/database-table-view.module.css
new file mode 100644
index 0000000..ae0c677
--- /dev/null
+++ b/apps/client/src/features/editor/components/database-table/database-table-view.module.css
@@ -0,0 +1,38 @@
+/* Schwebende Werkzeug-Blase (erscheint bei Node-Selektion, zentriert über dem Block) –
+   nutzt zusätzlich die geteilte toolbar-menu.module.css für die Blasen-Optik. */
+.bubble {
+  position: absolute;
+  top: -16px;
+  left: 50%;
+  transform: translateX(-50%);
+  z-index: 20;
+}
+
+/* Link-Buttons (öffnen / bearbeiten) sollen keinen Unterstrich haben. */
+.iconLink,
+.iconLink:hover {
+  text-decoration: none;
+}
+
+/* ---- Rahmen-Modi (deterministisch per CSS, Mantine-Border-Props sind aus) ---- */
+.bAll,
+.bH,
+.bNone {
+  border-collapse: collapse;
+}
+.bAll :is(th, td) {
+  border: 1px solid light-dark(var(--mantine-color-gray-3), var(--mantine-color-dark-4)) !important;
+}
+.bH :is(th, td) {
+  border-top: 1px solid light-dark(var(--mantine-color-gray-3), var(--mantine-color-dark-4)) !important;
+  border-bottom: 1px solid light-dark(var(--mantine-color-gray-3), var(--mantine-color-dark-4)) !important;
+  border-left: 0 !important;
+  border-right: 0 !important;
+}
+.bH thead th {
+  border-top: 0 !important; /* kein Rahmen oben über der Kopfzeile */
+}
+.bNone :is(th, td) {
+  border: 0 !important;
+}
+
diff --git a/apps/client/src/features/editor/components/database-table/database-table-view.tsx b/apps/client/src/features/editor/components/database-table/database-table-view.tsx
index db8dc0fd..08d6111d 100644
--- a/apps/client/src/features/editor/components/database-table/database-table-view.tsx
+++ b/apps/client/src/features/editor/components/database-table/database-table-view.tsx
@@ -350,9 +350,9 @@ export default function DatabaseTableView(props: NodeViewProps) {
             gIdx++; i = j;
           }
         }
-        // Eigene Hintergrundfarben für die verbundenen Gruppenzellen, alternierend pro GRUPPE
-        const GRP_BG_A = "light-dark(var(--mantine-color-gray-0), var(--mantine-color-dark-6))";
-        const GRP_BG_B = "light-dark(var(--mantine-color-gray-2), var(--mantine-color-dark-5))";
+        // Gruppen-Zellen: alternieren pro GRUPPE in den GLEICHEN Tönen wie das Body-Striping (weiß / grau)
+        const GRP_BG_A = "transparent";
+        const GRP_BG_B = "light-dark(var(--mantine-color-gray-0), var(--mantine-color-dark-6))";
 
         return (
           <div ref={scrollRef} draggable={false} onDragStart={(e) => e.preventDefault()} style={{ userSelect: "text", cursor: "default", overflowX: "auto" }} onMouseDown={onMouseDown} onMouseMove={onMouseMove} onMouseUp={onMouseUp} onDoubleClick={selectContainer}>
